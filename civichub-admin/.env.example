# Development can omit this and use the Vite /api proxy.
# Staging and production builds must provide the deployed HTTPS API origin.
VITE_API_URL=https://api.example.com

# Local release smoke tests only. Never enable this in CI or production.
# CIVICHUB_ALLOW_LOCAL_API_FOR_BUILD=true
